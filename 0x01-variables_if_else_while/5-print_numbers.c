#include <stdio.h>
/**
 * main - Entry point, where code execution begins
 * Return: Always 0 value (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}
